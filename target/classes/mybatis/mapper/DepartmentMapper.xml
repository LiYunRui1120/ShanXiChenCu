<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springbootdemo1.mapper.DepartmentMapper" >

    <select id="departmentList" resultType="Department">
        SELECT * from boot.department where id=#{depaId}
    </select>
    <select id="departmentListData" resultType="Department">
        SELECT * from boot.department where id=#{depaId}
    </select>
    <select id="adminList" resultType="Department">
        select* from boot.department
    </select>
    
</mapper>